#include<stdio.h>
#include<cs50.h>
 int i=card_number;
 int s;
 int lastdigit=0;
 int length = strlen(str);
 long long card_number;
 int sum1;
 int sum2;
 int result;
 int main(void)
 {

   do
   {
     printf(card_number<0);
     card_number = get_long_long();
   }

    while(crd_number<0);

       while(card_number !0)

       //last digit
       last_digit=card_number % 10;

       // second to last digit
       s += last digit;
        //multiply by 2
        // for(result+card_number/10;result>0;result/=100)
        //{
        //resullt=[s+2] * 2;
        //sum1=relsult = result ;
        //}
        //sum2 += s-1;
        //sum=sum1=sum2;




     if(sum%10==0)


     {
       if ((i >=340000000000000 && i<350000000000000 || (i>=370000000000000 && i<380000000000000))
        printf("This is an American Expree card\n");


        else if(i>=510000000000000 && i<560000000000000)
        printf("This is a Master card\n");

        else if((i>=4000000000000 && i<5000000000000) || (i>=4000000000000000 && i<5000000000000000))
        else
        printf("This is not a valid card \n");
     }
 }
     }